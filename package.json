{
  "name": "u-download",
  "private": true,
  "version": "2.2.0",
  "type": "module",
  "description": "Fast, cross-platform YouTube downloader with GUI",
  "author": "Dedan Okware <softengde@gmail.com>",
  "license": "MIT",
  "homepage": "https://github.com/okwareddevnet/u-download",
  "repository": {
    "type": "git",
    "url": "https://github.com/okwareddevnet/u-download.git"
  },
  "bugs": {
    "url": "https://github.com/okwareddevnet/u-download/issues"
  },
  "keywords": [
    "youtube",
    "downloader",
    "desktop",
    "tauri",
    "rust",
    "react"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:icons": "node scripts/make-icons.cjs",
    "tauri": "tauri",
    "tauri:build": "tauri build",
    "tauri:dev": "tauri dev",
    "version:patch": "./scripts/version-sync.sh $(node -e \"let v=require('./package.json').version.split('-')[0].split('.'); v[2]=(+(v[2]||0))+1; console.log([v[0],v[1],v[2]].join('.'))\")",
    "version:minor": "./scripts/version-sync.sh $(node -e \"let v=require('./package.json').version.split('-')[0].split('.'); v[1]=(+(v[1]||0))+1; v[2]=0; console.log([v[0],v[1],v[2]].join('.'))\")",
    "version:major": "./scripts/version-sync.sh $(node -e \"let v=require('./package.json').version.split('-')[0].split('.'); v[0]=(+(v[0]||0))+1; v[1]=0; v[2]=0; console.log([v[0],v[1],v[2]].join('.'))\")",
    "release:patch": "npm run version:patch && V=$(node -p \"require('./package.json').version\") && git add . && git commit -m \"chore: release v$V\" && git tag -fa latest -m latest && git tag -a v$V -m \"Release v$V\" && git push origin HEAD && git push -f origin latest && git push origin v$V",
    "release:minor": "npm run version:minor && V=$(node -p \"require('./package.json').version\") && git add . && git commit -m \"chore: release v$V\" && git tag -fa latest -m latest && git tag -a v$V -m \"Release v$V\" && git push origin HEAD && git push -f origin latest && git push origin v$V",
    "release:major": "npm run version:major && V=$(node -p \"require('./package.json').version\") && git add . && git commit -m \"chore: release v$V\" && git tag -fa latest -m latest && git tag -a v$V -m \"Release v$V\" && git push origin HEAD && git push -f origin latest && git push origin v$V"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.3.3",
    "@tauri-apps/plugin-opener": "^2.2.3",
    "@tauri-apps/plugin-store": "^2.4.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-react": "^4.6.0",
    "autoprefixer": "^10.4.21",
    "jimp": "^0.22.10",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "vite": "^7.0.4"
  }
}
